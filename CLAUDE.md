@AGENTS.md

# このプロジェクトの進め方（Claude 向けルール）

## まず最初に
- セッション開始時、`HANDOFF.md` を読んで経緯を把握してから作業を始める。

## ⚠️ git push は必ず確認してから（Auto Mode でも例外なし）
- `git push` を実行する前に、**必ず「プッシュしてよいですか？」とユーザーに確認する**。
- コミット（`git commit`）はしてOK。push の直前で必ず止まること。
- ユーザーが明示的に「プッシュして」と言った場合のみ即実行してよい。
- この規則は Auto Mode・どのモデルでも例外なく適用する。

## HANDOFF.md の運用（重要）
- このプロジェクトは `HANDOFF.md` を「次のセッションへの引き継ぎメモ」として使う。
- **作業に区切りがついたタイミングで、`HANDOFF.md` の更新を自分から提案する**こと。
  区切りの例：機能の一段落、フェーズ完了、まとまったコミットの後、ユーザーが「区切る／今日はここまで」と言ったとき。
- 更新する内容：
  - 「1. ここまでにやったこと」に新しい成果（コミットハッシュ付き）を追記
  - 「4. 次のステップ」の進捗・次にやることを最新化
  - 必要なら「最終更新」日付も更新
- ユーザーはIT初心者寄り。専門用語は避けるか、避けられないときは短く補足する。

## 触ってはいけないもの
- 現行WordPress（`~/Desktop/30nen_pj` および本番サーバー `the30nen.xsrv.jp`）は移行完了まで**読み取りのみ**。編集・削除・設定変更はしない。
