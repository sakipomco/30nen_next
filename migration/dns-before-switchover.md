# 切替前のDNS設定の控え（2026-07-28 23:15 取得）

> **これは何のためのファイル？**
> 7/29のドメイン切替がうまくいかなかった時、**元に戻すための「正解」**です。
> Xサーバーの画面で撮ったスクショと合わせて、二重の控えとして残します。
>
> 切り戻しの手順は `switchover-runbook.md` §7。
> **`30nen.com` と `www.30nen.com` のAレコードを `162.43.121.89` に戻せば、旧サイトがそのまま復活します。**

## いちばん大事な値

| 名前 | 種別 | 切替前の値（＝戻す先） | 切替後の値 |
|------|------|----------------------|-----------|
| `30nen.com`（ホスト名なし＝root） | A | **`162.43.121.89`**（旧Xserver） | `162.43.43.144`（VPS） |
| `www.30nen.com` | A | **`162.43.121.89`**（旧Xserver） | `162.43.43.144`（VPS） |
| `new.30nen.com` | A | `162.43.43.144`（VPS） | **触らない**（そのまま） |

- `162.43.121.89` … 旧サイト（Xserver・現行WordPress）
- `162.43.43.144` … 新サイト（VPS）

## 触ってはいけないレコード（メールが止まります）

| 名前 | 種別 | 値 |
|------|------|-----|
| `30nen.com` | MX | `0 30nen.com.` |
| `30nen.com` | TXT (SPF) | `v=spf1 +a:sv14288.xserver.jp +a:30nen.com +mx include:spf.sender.xserver.jp ~all` |
| `_dmarc.30nen.com` | TXT | `v=DMARC1; p=none;` |
| `send` 系 | — | Resend（お知らせメール送信）用。画面で確認して**そのまま** |

## ネームサーバー（変更しない）

`ns1.xserver.jp` / `ns2.xserver.jp` / `ns3.xserver.jp` / `ns4.xserver.jp` / `ns5.xserver.jp`

## 確認のしかた

切替後、下記が `162.43.43.144` を返せば成功：

```bash
dig +short 30nen.com
dig +short www.30nen.com
```

## 注意

このファイルは**インターネットから見える範囲**（公開DNS）を記録したものです。
Xサーバーの管理画面にしか出てこない設定（コメント欄・優先度・TTLの細かい値など）は
含まれないので、**画面のスクショも必ず併せて保存**してください。
