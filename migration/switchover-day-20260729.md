# 切替日タイムテーブル（2026-07-29・朝8時開始版）

作成: 2026-07-28 ／ もとになる台本: `switchover-runbook.md`（詳細コマンドは `production-import-procedure.md` 6章）

> この表は「**誰が・何時に・何をやるか**」だけをまとめた当日の進行表です。
> コマンドの詳細は台本（runbook）と手順書に書いてあるので、当日はこの表を上から順に消していけばOK。

## 役割分担（3人体制）

| 担当 | 役目 |
|------|------|
| **Opus**（Claudeのモデルのひとつ） | 実作業＝差分移行のコマンドを順に提示・実行。作業ログを残す |
| **Fable**（このモデル・いま話している私） | **チェック係**＝Opusの作業が完璧かを2回検査（DNS切替の前と後）。合格を出してから次へ進む |
| **SAKI** | 画面操作（XサーバーのDNS設定）・コマンド実行・試し書き記事の目視確認・書き手への連絡 |

> セッションの開き方メモ:
> - 朝いちばんに **新しいセッションをOpusで開き**、「switchover-day-20260729.md の §2 を上から進めて」と伝える。
> - Opusの作業が終わったら、**別の新しいセッションをFableで開き**、「switchover-day-20260729.md の §3（チェック①）をやって」と伝える。
> - Opusには作業結果を `migration/switchover-log-20260729.md`（当日ログ）に随時書いてもらう。Fableはそれを読んだうえで**自分でも独立に検証**する（ログを鵜呑みにしない）。

---

## §1. 今日（7/28）のうちにやること

### 1-A. 事前チェック（Opusと一緒に・30分〜1時間）

台本 §2 の点検デー。本番は止めない。

> 実施記録: 2026-07-28 14:00〜15:00（Fableと実施）

- [ ] **サーバーの「要再起動」を実施**（reboot → 2〜3分後に `https://new.30nen.com/` が 200 で返るか確認）
  → ⏳ **SAKI待ち**。「要再起動」フラグは立っている（32日連続稼働）。書き手のアクセスは13:35が最後で、
    午後は空いている＝いつでも実施可。Claudeからは安全装置でrebootを実行できないため、SAKIが下記を実行:
    `ssh -i ~/.ssh/30nen_vps root@162.43.43.144 reboot` → 2〜3分待って `https://new.30nen.com/` を開く
- [x] **テストデータの整理**: テスト管理者 `test@example.com` が本番に残っていないか確認 → 残っていれば削除
  → ✅ 本番DBに存在せず（整理済み）
- [ ] **SAKI SUZUKI（sakipomco）アカウントの要否を判断**（残せば「予備の管理者」・`remaining-tasks.md` F項）
  → ⏳ **SAKIの判断待ち**（ユーザー一覧の照会は個人情報のため安全装置でブロック。中身の確認は画面から）
- [x] **本文中に `new.30nen.com` の直URLが残っていないか確認**（SQLで件数カウント → 0でなければ相対URLに直す）
  → ✅ **0件**（対応不要）
- [x] **旧URLの301転送を本番で数件確認**
  → ⚠️ **不具合を発見し修正**（コミット `db502b4`）。転送先が `https://localhost:3000/posts/…` になっており、
    旧リンク・検索結果から来た読者が記事にたどり着けない状態だった。原因はNginxの裏側で
    `req.nextUrl.origin` が内部アドレスを返すこと。転送先を相対パス（`/posts/123`）に変更して解決。
    **⚠ 本番へのデプロイが未実施**（push待ち）
- [ ] **現在のDNSレコード一覧のスクショを保存**（Xサーバー「サーバーパネル＞ドメイン＞DNSレコード設定」＝**切り戻し時の正解**になる大事な控え）
  → ⏳ **SAKI待ち**（画面操作のためClaudeでは不可。切り戻しの命綱なので必ず今日中に）
- [x] **前夜のバックアップが正常か確認**（`/var/log/30nen-backup.log` の末尾）
  → ✅ 今朝 7/28 04:01 のDBバックアップ完了を確認（画像は7/26日曜に完了・正常）
- [x] **MacとVPSのコード（コミット）を一致させる**（未反映があれば push→deploy-prod.sh。※pushは必ずSAKIに確認）
  → ⏳ VPSは `13cb3a1` で2つ遅れ（`22816f2` 資料のみ＋`db502b4` 転送修正）。**push＋deploy がSAKI承認待ち**

### 1-B. 書き手への前日リマインド送信（SAKI・今夜のうちに）

- [x] 日本語版をLINE／メッセンジャーグループへ（文面: `writer-onboarding.md` §3）… ✅ 2026-07-28 送信済み
- [x] スペイン語版をLuis・Vanessaへ（同 §3。スペインは締切が**29日深夜1時**＝「28日の夜のうちに」と案内）… ✅ 2026-07-28 送信済み

---

## §2. 7/29 午前 — 差分移行（担当: Opus ＋ SAKI）

WordPressに本移行①（7/8頃）以降に増えた記事だけを新サイトへ追記する。
手順は **`production-import-procedure.md` 6章**（＝3章A〜Gの再実行）のとおり。`wp_id` で重複は自動で飛ぶ。

| 時刻 | やること |
|------|---------|
| **8:00** | 作業開始。Opusセッションを開く／WordPressへの投稿 締切（書き手に予告済み） |
| 8:00〜8:15 | **準備**: 事前バックアップ（DB `.backup`＋`.env.local`）・バックアップログ確認・VPS疎通確認 |
| **8:15** | **最新の書き出し開始**: `export-wp.sh`（読み取りのみ）→ rsync → `split-export.ts` → `derive-aux-files.ts` ※締切ギリギリ（7:59など）の投稿も確実に拾うため、8時ちょうどでなく8:15から |
| 8:45頃 | **画像を先にrsync**（差分だけ・無停止）＝手順書ステップA |
| 9:00頃〜 | **本番DBをMacへコピー → 記事を追記 → 本番へ戻す**＝ステップB〜G。⚠この30〜60分は**本番をいじらない**（投稿・プロフィール編集・招待をしない） |
| 　├ | **ステップEの前に `migrate-categories.ts` を実行**（新連載「欧州編」を作る。冪等なので安全） |
| 　├ | **試し書き記事の削除**（✅消すとSAKI決定済み）: wp_idが無い記事の一覧を出す → **SAKIが目視確認** → 削除 |
| 　└ | 検証 → 本番へ戻す → 再起動・表示確認 |
| 10:30頃 | Opus作業完了。当日ログ（`switchover-log-20260729.md`）を仕上げてもらう |

---

## §3. Fableチェック①＝DNS切替前の総点検（10:30頃〜・担当: Fable）

**ここが関所。全部合格するまでDNS切替に進まない。**

- [ ] **記事数の一致**: WordPressの公開記事数（読み取りのみで取得）＝ 新サイトの `wp_id` 付き記事数
- [ ] **最新記事**: 今朝8時直前までのWP投稿が新サイトに入っている（最新5件のタイトル・日時・著者を突き合わせ）
- [ ] **著者のひもづけ**: 差分で入った記事の書き手が正しい（author-map どおり）
- [ ] **連載のひもづけ**: 新連載「欧州編」が「度々の旅」の子として存在し、記事47702「旅のはじまりは。」が**欧州編側**に入っている
- [ ] **画像**: 差分記事の画像が表示される（数件抜き打ち＋本文中の壊れURL検索）
- [ ] **試し書き記事の削除確認**: wp_idが無い記事が0件（SAKIが残すと決めたものを除く）
- [ ] **本文中の `new.30nen.com` 直URLが0件**
- [ ] **旧URLの301転送**が生きている（数件）
- [ ] **バックアップが取れていること**（切り戻しの保険）

→ **全部✅ → SAKIに「合格。DNS切替に進んでOK」を報告** → §4へ
→ ひとつでも❌ → 原因を直してから再チェック（DNSは触らない。旧サイトが公開されたままなので焦らなくてよい）

---

## §4. 7/29 昼前後 — ドメイン切替（担当: SAKI ＋ Fable案内）

台本 §4 のとおり。目安 11:00頃開始。

1. [ ] **DNS向け替え**（SAKIがXサーバー画面）: `30nen.com`（root）と `www` のAレコードを `162.43.43.144` へ。⚠ `send`系・既存MX・`new` は**触らない**
2. [ ] `dig +short 30nen.com` が `162.43.43.144` を返すまで待つ（数分〜1時間）
3. [ ] Nginxに `30nen.com www.30nen.com` を追加 → `nginx -t` → reload
4. [ ] **https証明書を追加取得**（certbot ※DNSが向いてから）
5. [ ] `new.30nen.com` → `https://30nen.com` へ301転送に変更
6. [ ] `.env.local` に `SITE_URL=https://30nen.com`・`SITE_INDEXABLE=true` → `pm2 restart 30nen --update-env`

---

## §5. Fableチェック②＝切替後の最終確認（12:00頃〜・担当: Fable）

- [ ] `https://30nen.com/` → 200・トップが新サイト
- [ ] `www`・`http://` → httpsへ転送／`new.30nen.com` → `30nen.com` へ転送
- [ ] 旧URL → 記事へ301転送
- [ ] **ログイン → 試し投稿 → 画像アップロード → 公開反映**（店主アカウントで一連の流れ。試し投稿は確認後に削除）
- [ ] スマホ＋ダークモードで表示確認（書き手の主環境）
- [ ] `robots.txt` が Allow・`sitemap.xml` のURLが `30nen.com`
- [ ] 管理画面から「練習用」バナーが消えている

→ **全部✅ → 切替完了！** §6へ

---

## §6. 7/29 午後 — 完了アナウンスと見守り（担当: SAKI）

- [ ] 書き手へ「切替完了」連絡（文面: `writer-onboarding.md` §4。日本語＋スペイン語。スペインは朝6時頃）
  - 今後のログインは `30nen.com/login`／**新サイトはVPN不要**／プロフィールの誕生日・顔写真のお願い
- [ ] Google Search Console に `30nen.com` を登録 → sitemap.xml 送信（Fableが画面案内）
- [ ] HANDOFF.md を更新（切替完了の記録）
- 以降数日: 書き手のつまずき対応・表示くずれの見守り。旧WordPressは**数週間温存**（読み取りのみ）

---

## §7. うまくいかない時（切り戻し）

台本 §7 のとおり。**DNSのAレコードを元の値 `162.43.121.89` に戻せば旧サイトがそのまま復活**（§1-Aで撮ったスクショが正解）。
差分移行のDB切り戻しは `production-import-procedure.md` 5章。切り戻しても失うものはない。

---

## 当日の連絡体制

- 書き手からの質問窓口＝**SAKI**（LINE／メッセンジャー）
- Claudeのセッションを開いたままにして二人体制＝質問を貼れば返信下書き（日・西）や原因調べを即応
